import styled from 'styled-components';

export const NoneColorButton = styled.button`
  background: none;
  border: none;
  padding: 0px;

  &:focus {
    outline: none;
  }
`;

export const ClearBoth = styled.div`
  clear: both;
`;